package com.artemchep.acpods

import android.Manifest

/**
 * List of permissions required for app's
 * normal work.
 */
val REQUIRED_PERMISSIONS = listOf(
    Manifest.permission.ACCESS_COARSE_LOCATION
)
